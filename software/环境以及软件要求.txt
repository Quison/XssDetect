runtime : win7
python : 2.7.10
wxpython : 3.0.2.0


爬虫主要解析两类url，一种是匹配href，另外一种就是form表单，解析的地址可以考虑用redis来存在，速度快，难点在于去重。


请求采用requests库来做，可以参考我之前的那个爬虫程序，分有4个类来写


重点就是登录，cookie，代理IP，ajax请求等
登录可能会遇到验证码的情况，所以需要手动输入验证码，携带cookie进行提交
代理IP，这个可以爬取一些代理IP下载，requests可以实现使用代理IP进行请求爬取。
ajax请求，暂时还不知道怎么去做

至于反爬虫等可以用代理IP试试，暂时不做考虑，后续有时间再做


检测方面，就是吧请求的url加上payload进行二次请求，检测返回就好了。